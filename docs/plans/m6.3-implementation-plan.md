# M6.3 售后运行时实施计划

> 状态：M6.3-A、M6.3-B0 与 B1 已完成；B2-B7、UI 与生产启用未授权；M6.3 整体未完成
>
> 版本：0.6
>
> 日期：2026-07-29
>
> 依赖：`REQUIREMENTS.md`、`AGENTS.md`、`docs/plans/m6-implementation-plan.md`、
> `docs/reports/m6.2-completion-report.md`、`docs/database/m6-data-dictionary.md`、
> `docs/security/m6-permission-matrix.md`、`docs/api/openapi.m6.yaml`

## 1. 授权与阶段拆分

用户于 2026-07-28 要求按照仓库严谨工作流继续下一阶段。当前批准路线图唯一未开始的下一阶段为
M6.3；最初指令授权在既有 M6 契约、权限、资金、库存、物流与外部门禁内实施 M6.3-A，但不授权真实
供应商调用、生产凭据、生产政策、部署、推送或发布。M6.3-B 写路径还涉及下文列出的冻结契约冲突；
用户同日批准 `docs/plans/m6.3-b0-decision-plan.md` 的推荐默认值和 B0 契约/前向修复，该次批准本身
不授权 B1-B7。用户随后在了解风险后明确“按照建议执行”，另行只授权
`docs/plans/m6.3-b1-implementation-plan.md` 的 B1 四个只读接口；B2-B7 与外部动作仍不在授权范围。

M6.3 涉及 checkout、政策控制面、物流、售后、支付退款和 COD 结算多个高风险域，拆为两个独立
验收切片：

- M6.3-A 前置安全收口：checkout 政策解析/快照 writer、readiness/enforcement 控制面，以及既有
  M5 物流全链路 purpose 分流。本切片不创建返件或换货运单，不开放售后申请。
- M6.3-B 售后运行时：在 M6.3-A 门禁全部通过后，实施买家申请/取消、管理员审核、返件登记与可信
  物流事实、待验收读取、ONLINE Refund 权威事实协调和 COD 双人确认结算。完整返件验收写路径及
  exactly-once 库存恢复属于 M6.4。

M6.3-A 完成不等于 M6.3、M6、M5 或 P0 完成，也不自动授权 M6.4。

M6.3-A 实现与门禁进度（2026-07-28）：目标代码、四段前向迁移和专项文档已落地。定向 unit
55/55、M6.2 数据库 39/39、M4 15/15、M5.6 13/13、完整 integration 26 个文件/206 项，以及
39 段 M2→当前、重复部署、fresh、down/重新前滚和 `55000` 演练已通过；`verify`（54 个文件/381 项
单元测试）、21/21 E2E、交付候选 Gitleaks、`git diff --check` 与生产依赖 high 门禁均通过；审计另有
3 项 React Router moderate 公告并已明确结转。A 的证据与限制见
`docs/reports/m6.3-a-completion-report.md`。

最终物流复核补充关闭了创建/取消 outbox 并发窗口：供应商引用尚未生成时取消保持可重试，真实缺失
operation 仍永久失败；真实数据库同时覆盖两类非订单 purpose 的 shipment/tracking/operation 更新与
原订单 status/version/transitions 不变。

M6.3-B 前置只读差异审查发现返件 member transition、SYSTEM actor、ONLINE Refund 原子协调、验收与
M6.4 库存边界，以及多政策/金额/证据等冻结契约仍有不可同时满足之处。用户批准推荐默认值后，B0
已完成 domain/contracts/OpenAPI、前向数据库 guard/迁移、测试和文档收口；其范围、回滚和测试门禁见
`docs/plans/m6.3-b0-decision-plan.md`。

M6.3-B0 完成记录（2026-07-28）：domain/contracts 定向 35/35、B0/M6.2 数据库 44/44、M5.7 定向
9/9、完整 integration 26 个文件/211 项及 40 段迁移演练通过；`verify` 为 54 个文件/388 项单元测试，
生产依赖 high 门禁、交付候选 Gitleaks、`git diff --check` 和独立高风险复审均通过。B0 无新 UI 或
运行时，浏览器 E2E 为 `NOT_APPLICABLE`；3 项 React Router moderate 公告与后续协调/反向 guard 风险
明确结转。完整证据见 `docs/reports/m6.3-b0-completion-report.md`。

M6.3-B1 实现记录（2026-07-29）：只注册会员/管理员售后列表与详情四个 GET。读取采用严格 Prisma
`select` 与响应 allowlist；列表在 `REPEATABLE READ` 中先取 `limit + 1` 个 page key，再按白名单 ID
投影，并保留 PostgreSQL `timestamptz(6)` 六位微秒 tuple seek。会员显式 store+owner scope，管理员
要求 `store.after-sales.read` 和显式 store scope，两者叠加 FORCE RLS；独立 HMAC key ring 游标、
Redis 商城+主体 60/120 读限流、`Retry-After`、correlation ID 与 `private, no-store` 已同步实现。B1
不开放申请、取消、审核、凭证读取、返件、退款、COD 结算或任何其他写路由，也不交付 UI、worker、
生产政策/启用、供应商调用、部署或发布；B2-B7 继续未开始、未授权。

## 2. M6.3-A 目标与非目标

### 2.1 目标

- 在 checkout 创建订单的同一商城事务内，按“商品覆盖 → 最近主类目祖先 → 商城默认”解析当前活动、
  不可变售后政策版本，并为每个订单行写入规范 payload、SHA-256 和精确政策版本快照。
- enforcement OFF 时保持旧 checkout 兼容且不伪造历史快照；enforcement ON 时任何默认政策、活动
  assignment、解析或快照写入缺失都必须使下单事务失败。
- 实现当前商城 settings/readiness 读取和受审 enforcement 命令；启用必须重新执行服务端预检，校验
  独立权限、近期 MFA、确认词、expected version、AccessReason（平台跨商城时强制）、商城幂等、审计
  和商城/Header/查询一致性。
- 让订单物流查询、建单、取消、主动查单、callback hint、worker 和供应商事实应用显式识别
  `ShipmentPurpose`；只有 `ORDER_OUTBOUND` 可以推进原订单 `SHIP/DELIVER`。
- 对商城隔离、权限、并发、幂等、fail-closed 和非订单物流不推进订单提供自动化证据。

### 2.2 非目标

- 不创建、发布或启用任何生产售后政策；local/test fixture 不能被表述为越南合规结论。
- 不创建 `AFTER_SALE_RETURN` 或 `EXCHANGE_OUTBOUND` 运单；只先保证既有路径能安全分流。
- 不开放售后申请、审核、返件、ONLINE 退款或 COD 线下退款 API；其中申请、审核与可信返件事实属于
  M6.3-B，完整返件验收写路径及库存恢复属于 M6.4。
- 不实现证据对象存储/扫描、库存恢复、换货履约、会员中心、分享或 UI；分别属于后续切片/里程碑。
- 不改变订单、支付、退款、物流或售后状态机，不放宽 M5 真实供应商与 P0 上线硬门禁。

## 3. 涉及模块与接口

- `apps/api/src/checkout/checkout.service.ts`：政策解析和订单行快照同事务 writer。
- `apps/api/src/after-sales-policy/`：settings/readiness/enforcement 运行时 controller/service。
- `apps/api/src/app.module.ts`：仅注册本切片的运行时模块。
- `packages/database/src/after-sale-policy-primitives.ts`：稳定的商城事务原语、规范 hash、readiness 和
  enforcement 幂等/审计；readiness hash 同时绑定活动 assignment 与版本化 checkout runtime
  capability，应用升级不会沿用不兼容 writer 的旧 ready 事实。
- `packages/database/src/shipping-primitives.ts`、`shipping-callback-primitives.ts`：purpose-aware 读取、
  命令、callback 与权威事实投影。
- `apps/api/src/shipping/`、`apps/worker/src/shipments/`：显式传递和校验 purpose，避免非订单物流复用
  原订单推进路径。
- `packages/contracts/src/after-sales.ts` 与 `docs/api/openapi.m6.yaml`：只在实现发现冻结契约缺口时做
  兼容性收口，不接受客户端 ready、policy ID、store ID 或 purpose 选择来替代服务端解析。
- `tests/integration/` 及相邻单元测试：新增 M6.3-A 数据/API/并发/物流回归。

M6.3-A 当时只开放已冻结的：

- `GET /v1/admin/after-sale-settings`
- `PUT /v1/admin/after-sale-settings`

政策草稿、发布与停用 API 在 M6.3-B 或单独批准切片实现；M6.3-A 的 readiness 可对已迁移的
local/test fixture 或未来受审政策事实执行，但不会以数据库手改方式制造生产 ready。

### 3.1 M6.3-B1 只读运行时

B1 另行只开放：

- `GET /v1/after-sales`
- `GET /v1/after-sales/{afterSaleId}`
- `GET /v1/admin/after-sales`
- `GET /v1/admin/after-sales/{afterSaleId}`

会员列表固定 `created_at DESC, id DESC`，管理员固定 `updated_at DESC, id DESC`。两者都在同一个
`REPEATABLE READ` 商城事务中先用原生 SQL 读取 `limit + 1` 个 page key，再只对白名单 ID 使用严格
Prisma `select` 加载响应所需列并按 key 重排。page key 由 PostgreSQL 返回六位微秒 UTC 文本，下一页
直接使用 `(timestamp,id)` tuple seek，不把排序时间降级为 JavaScript 毫秒精度。

`c1_` 游标由 `AFTER_SALE_CURSOR_HMAC_KEYS` 的 1–3 把唯一 base64url HMAC-SHA-256 key ring 保护；
第一把签发、全部验证，并绑定版本、商城、主体、资源、规范筛选、微秒排序键、ID 与过期时间。会员/
管理员读限流分别为每 60 秒 60/120 次，绑定商城+主体且 Redis 故障失败关闭；成功响应统一
`Cache-Control: private, no-store`。B1 不注册任何写 handler。

## 4. 数据、迁移、兼容与回滚

- 优先复用 M6.2 已有模型、RLS、复合外键、不可变保护与 deferred enforcement guard；不改写历史
  订单，也不回填当前政策冒充历史快照。
- 四段 M6.3-A 前向迁移依次对齐最近主类目祖先的快照数据库 guard、为既有商城补稳定 OFF settings
  行、提供当前商城只读锁函数，并为后续新增商城自动 provision 同样的稳定 OFF 行。生产不创建
  默认政策，也不自动启用 enforcement。
- 若运行时实现暴露现有约束缺口，只添加可审查的向前迁移和 local/test `down.sql`；有业务事实的
  环境继续只允许向前修复。任何迁移必须加入 M2-to-current、重复 deploy、down/重新前滚和
  SQLSTATE `55000` 门禁演练。
- 应用回滚可关闭 settings 写入口并把未启用商城保持 OFF；已启用商城若回滚到没有 snapshot writer
  的应用版本，必须先通过受审命令关闭 enforcement，禁止直接改表或让 checkout 静默降级。
- shipping purpose 默认继续为 `ORDER_OUTBOUND`，兼容旧数据；所有新旧订单物流行为不变。非订单
  purpose 只停止推进原订单，不删除或改写运单/轨迹事实。
- B1 复用 M6.2/B0 售后表和 FORCE RLS，不新增业务列。前向迁移
  `20260728110000_m63_b1_after_sale_admin_read_index` 只增加管理员无 status 列表的
  `(store_id, updated_at DESC, id DESC)` 索引；应用回滚可保留该索引，受审 `down.sql` 只删除该精确
  索引。Prisma 只补记数据库既有 `after_sale_refunds(store_id, settlement_id)` 唯一约束，消除 schema
  drift 而不重复迁移。
- B1 应用回滚可取消四个 controller 注册，不删除任何售后事实。游标密钥回滚必须在最长 TTL 内保留
  所有仍可能验证有效游标的旧密钥；Redis 计数仅为短 TTL 限流状态，不是业务事实。

## 5. 风险、依赖与停止条件

- 越南业务/合规未批准美妆卫生、开封例外、期限、证据、运费和服装换码政策，所有商城
  enforcement 默认保持 OFF；没有批准时不得创建或发布生产政策。
- 真实两商城 ZaloPay/GHN、HTTPS callback、商户结算、GHN COD 回款和 Zalo 宿主仍为 M5/P0 上线
  硬门禁；本切片不能替代这些证据。
- readiness 必须基于服务端权威活动投影和可执行 checkout 预检生成 hash，不能信任客户端 ready、
  缓存旧结果或只检查“有一条默认 policy”。
- policy 解析冲突、缺失或 hash 不一致一律失败关闭；若无法在既有 M6.2 数据模型上证明解析唯一性，
  停止启用路径并先做数据/迁移设计复审。
- GHN callback 无签名，只能作为查单提示；purpose 不能来自 callback body，必须从商城隔离的本地
  shipment 事实读取。
- B1 读取的主要风险是跨商城/会员泄漏、宽投影暴露列级敏感字段、微秒排序精度丢失和密钥轮换中断。
  防护固定为显式 store/owner/RBAC + FORCE RLS、严格 `select`/response allowlist、数据库原生六位
  微秒 tuple seek，以及“新密钥置首、旧密钥保留至最长 TTL 后移除”的 1–3 把 key ring 流程。
- Redis 不可用时不得静默绕过 B1 限流；四个读取应失败关闭。B1 响应可见 settlement/evidence 的安全
  摘要不代表证据对象、退款协调或 COD 到账能力可用。

## 6. 测试与验收

- 单元：规范 JSON/hash、解析优先级、readiness 组成、enforcement 确认/版本/幂等、purpose 到订单
  事件门禁。
- 集成：双商城政策解析与订单行快照、OFF 兼容、ON 缺失 fail-closed、跨商城 assignment 拒绝、
  settings 并发和同键异参、RBAC/MFA/审计、非订单 shipment 事实不推进原订单。
- API：Header/查询商城一致性、严格 DTO、401/403/409、读取权限与 enforce 权限互不隐含。
- 回归：既有订单出库、取消、查询、callback/worker 继续工作；M5 支付退款和 M6.2 完整性测试不退化。
- 门禁：定向测试、`corepack pnpm verify`、完整 integration、相关 E2E、迁移演练、生产依赖审计、
  Gitleaks、`git diff --check` 和最终高风险差异审查。
- B1 专项覆盖严格 response/locale/evidence 投影、HMAC key ring 轮换与 scope、两阶段
  `limit + 1`/六位微秒分页、双商城/跨会员已知 UUID、RBAC/RLS、敏感 marker、60/120 限流、
  `Retry-After`、correlation 与 no-store。B1 没有 UI，专项浏览器 E2E 为 `NOT_APPLICABLE`；既有 E2E
  只能证明回归未退化，不能冒充售后 UI 或 Zalo 宿主真机验收。

M6.3-A、B0 与 B1 最终门禁已通过并保留各自独立报告，B1 最终计数见
`docs/reports/m6.3-b1-completion-report.md`。当前状态为“A/B0/B1 完成、B2-B7 未开始且未授权”。尚未安排的外部验收继续
记录为 `NOT_RUN/BLOCKED`；A/B0/B1 的局部交付不代表整个 M6.3 或 M6 完成。
